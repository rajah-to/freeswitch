<configuration name="xml_curl.conf" description="cURL XML Gateway">
  <bindings>
    <!--
      Bind to directory lookups — FS will POST to this URL
      when it needs to look up a user for registration/auth.
      Comment out sections you are not using.
    -->

    <!--
    <binding name="directory">
      <param name="gateway-url" value="http://your-api/freeswitch/directory"/>
      <param name="bindings" value="directory"/>
    </binding>
    -->

    <!--
    <binding name="dialplan">
      <param name="gateway-url" value="http://your-api/freeswitch/dialplan"/>
      <param name="bindings" value="dialplan"/>
    </binding>
    -->

    <!--
    <binding name="configuration">
      <param name="gateway-url" value="http://your-api/freeswitch/configuration"/>
      <param name="bindings" value="configuration"/>
    </binding>
    -->

  </bindings>
</configuration>